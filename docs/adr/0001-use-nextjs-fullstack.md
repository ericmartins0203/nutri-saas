# ADR 0001: Use Next.js as fullstack framework

We will use Next.js with TypeScript as the main fullstack framework for the MVP.

This keeps the project simple for a solo/early-stage product: routing, server actions, UI and deployment can live in one codebase. A separate backend can be introduced later only if the product outgrows the fullstack Next.js approach.
